<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.midterm.finalexamorderfood">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <uses-feature
        android:name="android.hardware.location.network"
        android:required="false" />
    <uses-feature
        android:name="android.hardware.location.gps"
        android:required="false" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.FinalExamOrderFood"
        tools:replace="android:theme">
        <activity android:name="com.finaltest.SearchActivity"></activity>
        <activity android:name="com.finaltest.OrderStatus" />
        <activity android:name="com.finaltest.FoodDetail" />
        <activity android:name="com.finaltest.FoodList" />
        <activity android:name="com.finaltest.showComment" />
        <activity
            android:name="com.finaltest.Home"
            android:label="@string/title_activity_home"
            android:theme="@style/Theme.FinalExamOrderFood.NoActionBar" />
        <activity android:name="com.finaltest.SignUp" />
        <activity android:name="com.finaltest.SignIn" />
        <activity android:name="com.finaltest.Cart" />
        <activity android:name="com.finaltest.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service android:name="com.finaltest.Service.MyFireBaseIdService">
            <intent-filter>
                <action android:name="com.google.firebase.INSTANCE_ID_EVENT" />
            </intent-filter>
        </service>
        <service android:name="com.finaltest.Service.MyFirebaseMessaging">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>
    </application>

</manifest>